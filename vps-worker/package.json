{
  "name": "truthstrike24-vps-worker",
  "version": "1.0.0",
  "description": "VPS worker for AI agent — generates news articles without Vercel timeout limits",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js --watch --every=60",
    "run": "node agent.js",
    "watch": "node agent.js --watch --every=60",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "dotenv": "^16.4.5",
    "jsonrepair": "^3.8.1",
    "prisma": "^6.19.3"
  },
  "engines": {
    "node": ">=20"
  }
}
